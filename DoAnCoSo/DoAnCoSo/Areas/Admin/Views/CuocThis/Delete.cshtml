@model DoAnCoSo.Models.tbCuocThi


<h1>Delete</h1>

<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.NgayThi)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.NgayThi)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.SoLuongThiSinh)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.SoLuongThiSinh)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.DiaDiem)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.DiaDiem)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.MonThi)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.MonThi.TenMonThi)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>
    <!-- Trong phần head của view hoặc layout -->
    <script>
        // Hàm hiển thị thông báo với thời gian tự đóng
        function showSuccessMessage() {
            Swal.fire({
                icon: 'success',
                title: '@TempData["SuccessMessage"]',
                showConfirmButton: false,
                timer: 1500,
                onClose: function () {
                    window.location.href = '@Url.Action("Index", "CuocThis")';
                }
            });
        }

        @if (TempData["SuccessMessage"] != null)
        {
            <text>
                showSuccessMessage();
            </text>
        }
    </script>
}
